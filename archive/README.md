# Archive

This folder keeps original scratch files, autosaves, and legacy example scripts from the pre-cleanup project.

The active solver, examples, tests, and documentation do not depend on anything in `archive/`. Files are kept here only for provenance and auditability while the project matures.

Archived content may be removed later after a deliberate review, but it is not part of the public API.

